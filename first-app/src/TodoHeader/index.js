import React from 'react';

function TodoHeader({ children
    /*totalTodos, 
    completedTodos,
    searchValue,
    setSearchValue*/
}){
return (
        <header>
           {children}
        </header>
    );

}
export { TodoHeader };